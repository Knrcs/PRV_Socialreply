//import schemas

module.exports = bot => {
    const getGuild = async (guild) => {

    };

    const updateGuild = async (guild, settings) => {

    };

    const createGuild = async (settings) => {

    };
};